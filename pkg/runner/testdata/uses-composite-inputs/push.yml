name: uses-composite-inputs
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: ./uses-composite-inputs/composite_action
      with:
        test_input_required: 'test_input_required_value'
